# CHANGELOG · 迭代记录

本文档记录「闻道 MindSpeak」的每次迭代更新。格式：

```
## [版本号] - 日期
### 新增 / 修复 / 优化 / 重构
- 条目
```

> 版本号维护在根目录 [`VERSION`](VERSION)，每次发布更新请在**顶部**追加一条记录。

---

## [1.0.0] - 2026-08-15

### 修复
- **安卓端朗读完全无声且无提示（第一版即修复）**：
  1. **空音频判定死代码**：远程发音并行抢播时，部分发音源（百度 gettts 在安卓 WebView 被 Referer 校验拒）会返回空 MP3/空 HTML，`<audio>` 能正常 `onplay` 却没有任何真实声音。旧代码的"空音频判定"用了 `(Date.now()-claimAt) < 400` 判断，而 `setTimeout(..., 400)` 回调触发时该差值恒 ≥400（定时器不可能提前触发），条件**永不成立** → 空音频会抢占"胜者"并停掉并行发起的有道，导致**完全无声、无任何提示**。现改为真正可触发的 700ms 看门狗（ended 且 `currentTime=0` ⇒ 空/损坏文件；700ms 无进度 ⇒ 播放卡住/WebView 无声），判定即换源兜底；并在 `onended` 路径补上同样的空音频保护（防止空音频极快 ended 绕过看门狗、整句无声却算成功）。
  2. **安卓端并行抢播源排除百度/谷歌**：并行候选集合（pickSrcs）此前仍包含百度/谷歌——百度 gettts 带页面 Referer 返回空音频、谷歌安卓 UA 返回 404，连了必失败还会抢占胜者。现安卓端并行候选只含有道美/英音（不校验 Referer、国内直连稳定出声），系统默认/本地男女声/在线音色全部统一走有道兜底链。
  3. **Android WebView 兼容：`new Audio()` 元素先挂到 DOM 再播放**：部分 WebView 里 JS 创建的音频元素不挂 DOM 会"播放成功但无声"（onplay 正常触发、无 error、无提示）。现所有音频播放路径（远程发音 / 字母 wav / 自定义录音 / server SAPI / Google 合成 / 试听按钮）统一在播放前挂到 `<body>`，结束/失败/换源后移除，避免残留。
- **远程发音源可用性**：有道美音/英音接口实测稳定返回音频（国内直连）；百度可用但受 Referer 限制、谷歌系墙内不可达，作为桌面端兜底保留。

### 说明
- 本次为**第一版正式发布**：清理了历史版本（旧 v1.0.0~v1.0.4 及 1.1.x 预览版）的 Release 与标签，统一以 v1.0.0 作为首个正式版本。

### 补充修复（发布后，未重新打包 APK）
- **README 下载链接失效**：下载按钮仍指向已删除的 v1.1.1（返回 404），已改为 v1.0.0 正式版链接，用户现在可直接下载到含全部安卓朗读修复的 APK。
- **安卓顶部"一条杠"**：打包脚本 step 名为"状态栏透明，消除顶部一条杠"，但实际 styles.xml 把系统状态栏设成了**不透明** #F4F6F8（页面 body 象牙色），而 App 顶部实际是**白色 header**（rgba(255,255,255,0.85)≈白），颜色不匹配 → 顶部露出浅灰长条。已改为 #FFFFFF + 深色状态栏图标，与白色 header 融为一体。
