# Review package: f3efa10..d8bf63ca65dfa8aad3b5c3ce740b3f05b03c0db3

## Commits
d8bf63c feat: add Android back button handler with priority chain

## Files changed
 js/app.js | 54 +++++++++++++++++++++++++++++++++++++++++++++++-------  1 file changed, 47 insertions(+), 7 deletions(-)

## Diff
diff --git a/js/app.js b/js/app.js index f3dd67d..ef5f67e 100644 --- a/js/app.js +++ b/js/app.js @@ -150,57 +150,59 @@ var SpeechUtil = {        // 瑙﹀彂涓€涓┖ utterance 浠?婵€娲?speechSynthesis锛?       // 缁熶竴缁?TTSManager.speak 鏈楄锛堜細濂楃敤鎵€閫夊０闊筹紝椤哄甫棰勭儹鍦ㄧ嚎澹拌繛鎺ワ級銆?       // immediate=true锛歩OS Safari 蹇呴』鍦ㄧ敤鎴锋墜鍔夸笂涓嬫枃閲岀珛鍗?speak锛屼笉鑳藉欢杩?100ms銆?       TTSManager.speak(' ', { volume: 0, rate: 0.5, immediate: true });        this._unlocked = true;      } catch(e) {}    },    // 鏈楄璁剧疆缂撳瓨锛歷oice_name锛堥璁惧０闊冲悕绉帮級銆乿oice_rate锛堣閫燂級銆乿oice_pitch锛堥煶璋冿級銆?   // instant锛堟槸鍚︽湰鍦板嵆鏃惰闊筹細涓嶄緷璧栧湪绾胯繛鎺ワ紝鐢?server SAPI 鍚堟垚锛屽嚑涔庨浂寤惰繜锛? getSettings: function() { -    if (this._settings) return this._settings; -    // 鍗虫椂璇煶榛樿鍊硷細鍙湁鐢ㄦ埛娌″姩杩?voice_instant 鏃舵墠鍋?鑷姩鎺㈡祴"锛?-    // 鏈湴鏈?server 鏃朵繚鎸佸紑锛堥浂寤惰繜锛夛紝GitHub Pages/鎵嬫満绛夋棤鍚庣鐜鑷姩鍏炽€?+    // 姣忔浠?localStorage 瀹炴椂璇诲彇锛岄伩鍏嶇紦瀛樿繃鏈熷鑷村０闊冲垏鎹笉鐢熸晥銆?+    // getSettings() 璋冪敤棰戠巼浣庯紙浠呮湕璇?璁剧疆椤碉級锛屾棤闇€缂撳瓨浼樺寲銆?     var hasInstant = false;      try { hasInstant = (window.localStorage.getItem('voice_instant') !== null); } catch(e) {}      var instant;      if (hasInstant) instant = Storage.get('voice_instant', '1') === '1';      else instant = !(this._serverDown === true); -    this._settings = { +    var s = {        voiceName: Storage.get('voice_name', ''),        rate: parseFloat(Storage.get('voice_rate', '')) || 0.9,        pitch: parseFloat(Storage.get('voice_pitch', '')) || 1,        instant: instant,        _defaultInstant: !hasInstant      }; -    return this._settings; +    this._settings = s; +    return s;    },    updateSettings: function(s) {      this._settings = s;    },    // 鎺㈡祴鏈湴 server 鏄惁鍙揪锛?api/health 杩斿洖 200 涓?ok=true銆?   // 缃戦〉閮ㄧ讲锛圙itHub Pages锛?鎵嬫満鎵撳紑鏃讹紝API_BASE 鍙兘鎸囧悜涓€涓病鏈夊悗绔殑绔欑偣锛?   // 鎺㈡祴澶辫触 鈫?鑷姩鎶?鍗虫椂璇煶"榛樿鍏虫帀銆佹敼璧版祻瑙堝櫒璇煶锛岄伩鍏嶆瘡娆℃湕璇婚兘寮圭孩    // "server 鏈繍琛?鎻愮ず銆傜敤鎴锋墜鍔ㄥ紑杩囧嵆鏃惰闊冲垯灏婇噸鐢ㄦ埛閫夋嫨涓嶄綔鏀瑰姩銆?   _probeServer: function() {      if (this._probedServer) return;      this._probedServer = true;      // Android WebView / Capacitor 鎵撳寘鐜锛氶〉闈㈠姞杞戒簬 https://localhost锛?     // 瀵?/api/health 鐨勮姹備細琚?Capacitor 鏈湴 server 浠?SPA 鍥為€€杩斿洖 HTML锛?00锛夛紝      // 鑰屼笉浼氳鍒ゅ畾涓?鍚庣瀛樺湪"鈫?璇蛋鍦ㄧ嚎 TTS 鍗′綇鏃犲０銆侫PK 鍐呮病鏈夋湰鍦?server锛?     // 鐩存帴鏍囪 _serverDown=true锛屽彂闊宠蛋杩滅▼ MP3/绯荤粺璇煶/鏈湴 wav 鍏滃簳閾俱€?     try {        var _ua = (navigator.userAgent || '').toLowerCase();        var _hc = document.documentElement.className || '';        var _isAndroid = /android/.test(_ua) || /platform-android/.test(_hc);        var _isCap = /capacitor|bridge\.capacitor|\(([^)]*net\.js\.[^)]*)/.test(navigator.userAgent || ''); -      if (_isAndroid || _isCap) { +      // Capacitor v3+ 娉ㄥ叆 window.Capacitor 鍏ㄥ眬瀵硅薄锛屾瘮 UA 妫€娴嬫洿鍙潬 +      var _isCapGlobal = !!(window.Capacitor && window.Capacitor.isNative); +      if (_isAndroid || _isCap || _isCapGlobal) {          this._serverDown = true;          this._diag('probe', 'android/capacitor => serverDown=true (APK 鍐呮棤鍚庣)');          this._syncInstantDefault();          return;        }      } catch(e) {}      var self = this;      var base = window.API_BASE || '';      this._diag('probe', 'base=' + base + ' href=' + location.href);      if (!base) { this._serverDown = true; this._syncInstantDefault(); return; } @@ -587,20 +589,23 @@ var attempt = function() {        if (online && typeof self._onlineBrokenAt === 'number' && (Date.now() - self._onlineBrokenAt < 15000)) {          // 杩炶閿佸畾锛氭湰娈佃繛璇诲悗缁叏閮ㄤ繚鎸?Google 鍚堟垚锛屽喎鍗存湡杩囦簡涔熶笉鍦ㄤ腑閫斿垏鍥?         self._burstMode = 'google';          self._burstUntil = Date.now() + 3000;          self._speakGoogleTTS(text, lang, opts);          return;        }        // 鑻辨枃鏂囨湰蹇呴』鐢ㄨ嫳鏂囧０锛氭湰鏈烘病鏈夎嫳鏂囧０鏃?_pickLocalENVoice 浼氬洖閫€鍒?浠绘剰闈炲湪绾垮０"        // 锛堝疄娴嬪洖閫€鎴愪腑鏂?Huihui锛夛紝鑻辨枃琚腑鏂囧０璇?鈫?璇讳笉鍑嗐€傛鏃剁洿鎺ヨ惤鏈湴 SAPI锛圸ira锛夛紝        // 绂荤嚎銆佸彂闊冲噯纭紱鍦ㄧ嚎鑻辨枃澹板彲鐢ㄦ椂涓嶅彈褰卞搷銆?+      // voice 涓?null 鏃讹紙鎵€鏈夊０闊抽兘澶辫触杩囷級锛屾竻闄ゆ敞鍏ョ殑鏃?voice 寮曠敤锛?+      // 閬垮厤 stale voice 琚紶缁?_speakLocal/_speakServerless 瀵艰嚧涓嶅彲棰勬湡琛屼负銆?+      if (!voice && opts) opts.voice = null;  var voiceEN = !!(voice && /^en/i.test(String(voice.lang || '')));        if (!voiceEN && /^en/i.test(String(lang || 'en-US'))) {          if (useDeviceVoice) {            self._burstMode = 'device';            self._burstUntil = Date.now() + 3000;            self._speakServerless(text, lang, opts);            return;          }          // 杩炶閿佸畾锛氭湰娈佃繛璇诲悗缁叏閮ㄤ繚鎸?SAPI 鏈湴鍚堟垚锛岄伩鍏嶄腑閫旀崲闊宠壊          self._burstMode = 'local'; @@ -1723,20 +1728,29 @@ var DataStore = {      var idx = Math.min(rc, this.ebbinghausIntervals.length - 1);      var days = this.ebbinghausIntervals[idx];      // 鑷€傚簲妯″紡锛?鏈夌偣鍗拌薄"鎸夎〃闂撮殧鍑忓崐鎺ㄨ繘锛堣嚦灏?1 澶╋級      if (half) days = Math.max(1, Math.ceil(days / 2));      var next = new Date();      next.setDate(next.getDate() + days);      return getLocalDateStr(next);    }  };   +if (typeof window !== 'undefined') { +  window.escapeHtml = escapeHtml; +  window.getLocalDateStr = getLocalDateStr; +  window.calculateStreak = calculateStreak; +  window.Storage = Storage; +  window.DataStore = DataStore; +  window.SpeechUtil = SpeechUtil; +} +  // ==================== 涓婚鍒囨崲 ====================  var ThemeToggle = {    init: function() {      var saved = Storage.get('theme', 'light');      this.apply(saved);      var btn = document.getElementById('btn-theme-toggle');      if (btn) btn.addEventListener('click', function() { ThemeToggle.toggle(); });    },    apply: function(theme) {      document.documentElement.setAttribute('data-theme', theme); @@ -5256,21 +5270,26 @@ function App() {    this.itemSystem = init('ItemSystem', function() { return new ItemSystem(); });    this.themeShop = init('ThemeShop', function() { return new ThemeShop(); });    this.listenAlongModule = init('ListenAlongModule', function() { return new ListenAlongModule(); });    this.badgeSystem = init('BadgeSystem', function() { return new BadgeSystem(); });    this.dailyPlanModule = init('DailyPlanModule', function() { return new DailyPlanModule(); });    this.dashboardModule = init('DashboardModule', function() { return new DashboardModule(); });    this.aiCoachModule = init('AiCoachModule', function() { return new AiCoachModule(); });    this.aiChatModule = init('AiChatModule', function() { return new AiChatModule(); });    this.assessmentModule = init('AssessmentModule', function() { return new AssessmentModule(); });    this.onboardingModule = init('OnboardingModule', function() { return new OnboardingModule(); }); -  App.detectPlatform(); +  var platform = App.detectPlatform(); +  var isAndroid = platform === 'android'; +  var isCap = !!(window.Capacitor && window.Capacitor.isNative); +  if (isAndroid || isCap) { +    this._initAndroidBackHandler(); +  }    this.initNav();    this.currentTab = 'home';    this.updateGlobalStats();    this.bindSettings();  }  App.detectPlatform = function() {    var ua = (navigator.userAgent || '').toLowerCase();    var html = document.documentElement;    var isIOS = /iphone|ipad|ipod/.test(ua) || (/macintosh/.test(ua) && navigator.maxTouchPoints > 1);    var isAndroid = /android/.test(ua); @@ -5395,20 +5414,41 @@ App.prototype._initMobileDrawer = function() {        var lastEl = navs[navs.length - 1];        if (e.shiftKey && document.activeElement === firstEl) { e.preventDefault(); lastEl.focus(); }        else if (!e.shiftKey && document.activeElement === lastEl) { e.preventDefault(); firstEl.focus(); }      }    });    // Android 杩斿洖閿細鎶藉眽鎵撳紑鏃朵紭鍏堝叧鎶藉眽    window.addEventListener('popstate', function() {      if (sidebar.classList.contains('open')) close(false);    });  }; +App.prototype._initAndroidBackHandler = function() { +  if (!window.Capacitor || !window.Capacitor.Plugins || !window.Capacitor.Plugins.App) return; +  var self = this; +  window.Capacitor.Plugins.App.addListener('backButton', function() { +    var sidebar = document.getElementById('sidebar'); +    if (sidebar && sidebar.classList.contains('open')) { +      sidebar.classList.remove('open'); +      return; +    } +    var modal = document.querySelector('.modal-overlay.active'); +    if (modal) { +      modal.classList.remove('active'); +      return; +    } +    if (window.history.length > 1) { +      window.history.back(); +      return; +    } +    Toast.info('鍐嶆寜涓€娆￠€€鍑?); +  }); +};  App.prototype.showTab = function(tab) {    this.currentTab = tab;    // 绉诲姩绔細閫変腑鍚庤嚜鍔ㄦ敹璧锋娊灞夊鑸紝骞跺洖鏀?Android 杩斿洖閿崰鐢ㄧ殑 history 鐘舵€?   if (this._drawerEls && this._drawerEls.sidebar) {      this._drawerEls.sidebar.classList.remove('open');      if (this._drawerEls.scrim) this._drawerEls.scrim.classList.remove('show');      try {        if (window.history && window.history.state && window.history.state.msDrawer) window.history.back();      } catch (e) {}    }
